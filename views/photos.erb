<div class="container">
  <div class="row">
    <% @photos.all.each do |photo| %>
      <div class="col-sm-6 col-md-4">
        <img class="img-responsive" src="<%= photo.file.url %>">
      </div>
    <% end %>
  </div>
</div>
